# generated by generate-github-actions
# Do not edit.

name: build-image-node-18

concurrency:
  group: "devcontainers.nix-build-image-node-18"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-node-18-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: node-18
      free-disk-space: false



  build-node-18-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: node-18


  manifest:
    needs:

      - build-node-18-amd64


      - build-node-18-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: node-18
