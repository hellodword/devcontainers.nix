# generated by generate-github-actions
# Do not edit.

name: build-image-cpp

concurrency:
  group: "devcontainers.nix-build-image-cpp"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-cpp-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: cpp
      free-disk-space: true



  build-cpp-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: cpp
      free-disk-space: true


  manifest:
    needs:

      - build-cpp-amd64


      - build-cpp-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: cpp
