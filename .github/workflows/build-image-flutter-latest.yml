name: build-image-flutter-latest

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - flake.nix
      - flake.lock
      - images/**
      - lib/**
      - runtime/**
      - tests/**
      - .github/workflows/**

jobs:
  build:
    uses: ./.github/workflows/_build-image.yml
    with:
      image-target: flutter-latest
