# generated by generate-github-actions
# Do not edit.

name: build-image-dotnet-10

concurrency:
  group: "devcontainers.nix-build-image-dotnet-10"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-dotnet-10-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: dotnet-10
      free-disk-space: true



  build-dotnet-10-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: dotnet-10


  manifest:
    needs:

      - build-dotnet-10-amd64


      - build-dotnet-10-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: dotnet-10
