# generated by generate-github-actions
# Do not edit.

name: build-image-node

concurrency:
  group: "devcontainers.nix-build-image-node"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-node-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: node
      free-disk-space: false



  build-node-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: node


  manifest:
    needs:

      - build-node-amd64


      - build-node-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: node
