# generated by generate-github-actions
# Do not edit.

name: build-image-java-8

concurrency:
  group: "devcontainers.nix-build-image-java-8"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-java-8-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: java-8
      free-disk-space: true



  build-java-8-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: java-8
      free-disk-space: true


  manifest:
    needs:

      - build-java-8-amd64


      - build-java-8-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: java-8
