# generated by generate-github-actions
# Do not edit.

name: build-image-latex

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-latex-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: latex
      free-disk-space: false



  build-latex-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: latex


  manifest:
    needs:

      - build-latex-amd64


      - build-latex-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: latex
