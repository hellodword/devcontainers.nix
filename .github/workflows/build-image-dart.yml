# generated by generate-github-actions
# Do not edit.

name: build-image-dart

concurrency:
  group: "devcontainers.nix-build-image-dart"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-dart-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: dart
      free-disk-space: true



  build-dart-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: dart
      free-disk-space: true


  manifest:
    needs:

      - build-dart-amd64


      - build-dart-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: dart
