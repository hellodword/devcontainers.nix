# generated by generate-github-actions
# Do not edit.

name: build-image-java

concurrency:
  group: "devcontainers.nix-build-image-java"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-java-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: java
      free-disk-space: true



  build-java-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: java
      free-disk-space: true


  manifest:
    needs:

      - build-java-amd64


      - build-java-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: java
