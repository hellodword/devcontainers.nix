# generated by generate-github-actions
# Do not edit.

name: build-image-go

concurrency:
  group: "devcontainers.nix-build-image-go"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-go-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: go
      free-disk-space: true



  build-go-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: go
      free-disk-space: true


  manifest:
    needs:

      - build-go-amd64


      - build-go-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: go
