# generated by generate-github-actions
# Do not edit.

name: build-image-go-zigcc

concurrency:
  group: "devcontainers.nix-build-image-go-zigcc"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-go-zigcc-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: go-zigcc
      free-disk-space: false



  build-go-zigcc-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: go-zigcc


  manifest:
    needs:

      - build-go-zigcc-amd64


      - build-go-zigcc-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: go-zigcc
