# generated by generate-github-actions
# Do not edit.

name: build-image-python

concurrency:
  group: "devcontainers.nix-build-image-python"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-python-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: python
      free-disk-space: true



  build-python-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: python
      free-disk-space: true


  manifest:
    needs:

      - build-python-amd64


      - build-python-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: python
