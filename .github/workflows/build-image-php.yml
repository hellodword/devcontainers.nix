# generated by generate-github-actions
# Do not edit.

name: build-image-php

concurrency:
  group: "devcontainers.nix-build-image-php"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-php-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: php
      free-disk-space: false



  build-php-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: php


  manifest:
    needs:

      - build-php-amd64


      - build-php-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: php
