# generated by generate-github-actions
# Do not edit.

name: build-image-nix

concurrency:
  group: "devcontainers.nix-build-image-nix"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-nix-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: nix
      free-disk-space: false



  build-nix-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: nix


  manifest:
    needs:

      - build-nix-amd64


      - build-nix-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: nix
