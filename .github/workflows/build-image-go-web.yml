# generated by generate-github-actions
# Do not edit.

name: build-image-go-web

concurrency:
  group: "devcontainers.nix-build-image-go-web"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-go-web-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: go-web
      free-disk-space: false



  build-go-web-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: go-web


  manifest:
    needs:

      - build-go-web-amd64


      - build-go-web-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: go-web
