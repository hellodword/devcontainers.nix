# generated by generate-github-actions
# Do not edit.

name: build-image-nginx

concurrency:
  group: "devcontainers.nix-build-image-nginx"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-nginx-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: nginx
      free-disk-space: true



  build-nginx-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: nginx
      free-disk-space: true


  manifest:
    needs:

      - build-nginx-amd64


      - build-nginx-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: nginx
