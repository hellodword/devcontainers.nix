# generated by generate-github-actions
# Do not edit.

name: build-image-go-cc

concurrency:
  group: "devcontainers.nix-build-image-go-cc"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-go-cc-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: go-cc
      free-disk-space: false



  build-go-cc-arm64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-24.04-arm
      package: go-cc


  manifest:
    needs:

      - build-go-cc-amd64


      - build-go-cc-arm64

    uses: ./.github/workflows/manifest.yml
    with:
      package: go-cc
