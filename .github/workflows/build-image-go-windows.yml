# generated by generate-github-actions
# Do not edit.

name: build-image-go-windows

concurrency:
  group: "devcontainers.nix-build-image-go-windows"
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["master"]

# # buggy https://github.com/orgs/community/discussions/41518
# concurrency:
#   group: "hellodword-devcontainers-nix"
#   cancel-in-progress: false

jobs:


  build-go-windows-amd64:
    uses: ./.github/workflows/build.yml
    with:
      runs-on: ubuntu-latest
      package: go-windows
      free-disk-space: true




  manifest:
    needs:

      - build-go-windows-amd64


    uses: ./.github/workflows/manifest.yml
    with:
      package: go-windows
